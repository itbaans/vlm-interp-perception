[pytest]
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
markers =
    order(n): documents that these script smoke tests run in file order, each
        consuming what the previous produced. pytest already runs them in
        definition order; the marker records the dependency.
