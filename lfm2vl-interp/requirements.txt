# Pinned to the version the LFM2.5-VL-3B checkpoint was published with. This matters:
# the repo uses transformers v5 conventions (TokenizersBackend, rope_parameters, no
# special_tokens_map.json), and transformers 4.x cannot load its processor at all.
transformers==5.8.1

torch>=2.4
# Required, not optional: Lfm2VlImageProcessor is a fast (torchvision-backed) image
# processor, so AutoProcessor.from_pretrained fails outright without this.
torchvision
accelerate>=1.0
pillow>=10.0
numpy>=1.24
pyyaml>=6.0
tqdm>=4.66
# Figures for the report bundle (src/plots.py).
matplotlib>=3.8

# Dev only.
pytest>=8.0

# Optional. Only needed for RLE-encoded (iscrowd=1) COCO annotations, which the paper's
# filter excludes anyway; polygon segmentations are rasterized with Pillow.
# pycocotools>=2.0.8
